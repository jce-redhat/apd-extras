---
aap_user_accounts:
  - username: apd-admin
    email: apd-admin@example.invalid
    password: '{{ lookup("env", "AAP_PASSWORD") | default(vault_aap_password) | default("CHANGE_ME_NOW") }}'
    update_secrets: false

gateway_role_user_assignments:
  - user: apd-admin
    role_definition: Organization Admin
    object_ids:
      - Ansible Product Demos (APD)

...
